import 'package:flutter/material.dart';

class OrgainzationHome extends StatelessWidget {
  const OrgainzationHome({super.key});
  static String id = 'OrgHome';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [],
      ),
    );
  }
}
